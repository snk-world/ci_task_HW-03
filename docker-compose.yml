version: '3'
services:
    nginx:
        image: nginx:latest
        ports:
            - "9889:80"
        volumes:
          #- ./hosts:/etc/nginx/conf.d
            - ./www:/var/www
            - ./logs:/var/log/nginx
